pipeline (старт - текущие сутки, конец - когда укажем):
0) Создаём пустой фрейм под итоговый результат
--------------------------------------------------------------------------------------------------------
1) генерируем новый пустой фрейм, прибавляем к нему последние 128 строк (7 * 24) от df_general
2) {удаляем столбцы лагов} делаем препроцессинг для последних 24 часов
3) ограничиваем выборку в одни сутки (просто удаляем все NaN)
4) определяем лучшие гиперпараметры модели для текущей версии генеральной совокупности
5) получаем прогнозные значения (Volume) на текущие сутки используя ранее найденные гиперпараметры
6) пополняем генеральную совокупность текущими сутками (24 часа)
7) добавляем полученные прогнозные значения в итоговый прогнозный фрейм (трансформируем get_df_predicted)
-------делаем смещение в 1 сутки и повторяем пункты (1–7) в соответствии с горизонтом планирования-------
8) Выводим итоговый прогнозный фрейм (столбцы Date и Predicted)